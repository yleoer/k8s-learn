# k8s 亲和力

如何把同一个应用分配在不同节点，分配到不同的机房，减少一个节点/机房挂掉，降低对业务的影响。

- 某些 Pod 优先选择有 ssd=true 标签的节点，如果没有再考虑部署到其他节点
- 某些 pod 需要部署在 ssd=true和type=physical的节点上，但是优先部署在ssd=true的节点上
- 同一个应用的不同副本或者同一个项目的应用尽量或必须不部署在同一个节点或符合某个标签的一类节点上或不同的区域
- 相互依赖的两个pod尽量或必须部署在同一个节点上或者同一个域内

Pod和节点之间的关系，Pod和Pod之间的关系。

亲和力（必须部署在一起）和反亲和力（不能部署在一起）的关系

## 什么是亲和力 Affinity

亲和力是一种调度规则，作用于Pod资源，用于控制Pod的调度行为，比如Pod应该或者尽量调度到哪些节点上/Pod不能和哪些Pod部署在一起等，从而实现更灵活的资源管理和故障隔离。

亲和力支持三种类型：节点亲和力（NodeAffinity）Pod亲和力（PodAffinity）和Pod反亲和力（PodAntiAffinity）

- 节点亲和力：用于控制Pod和节点之间的关系
- Pod亲和力：用于控制Pod和Pod之间的关系，允许和某些Pod部署在同一个位置
- Pod反亲和力：同样用于控制Pod和Pod之间的关系，不允许和某些Pod部署在同一个位置

同时亲和力也分为强制和非强制调度策略，用于实现调度规则是属于尽量满足还是必须满足。

## 常见使用场景

- 同一个应用不同副本调度到不同的节点，增加服务的高可用性
- 同一个应用不同副本调度到不同的机房，增大容灾能力
- 同一个项目的不同应用尽量分配到不同位置，增加项目的高可用性
- 特殊应用尽量分配到专用节点，提升应用性能
- 相互依赖的服务，分配到同一个域内

## 拓扑域和拓扑键

拓扑域（Topology Domain）通常用于标识一组具有相似属性/相似网络特性的节点，这些节点通常位于同一个物理位置或者某个网络子网中。拓扑域一般用于亲和力配置，用于优化资源分配和提高系统的高可用性。

拓扑键（Topology Key）用于指定拓扑域，比如指定Topology Key 为 failure-domain.beta.kubernetes.io/zone，即表示按照具有该标签的拓扑域作为作用范围。

基于主机划分拓扑域 kubernetes.io/hostname

基于多区域划分拓扑域：region

基于数据中心划分拓扑域：zone

## 配置

//TODO

//示例1：同一个应用必须部署在不同的宿主机

//示例2：同一个应用尽量部署在不同的宿主机

//示例3：同一个应用分布在不同的机房

//示例4：应用尽量和缓存服务部署在同一个可用域

//示例5：计算服务必须部署在高性能机器

//示例6：计算服务尽量优先部署在高性能机器

//示例6：应用尽量不部署在某些机器上

//示例6：拓扑域约束topologySpreadConstraints

//示例6：应用均匀分布在多个机房



