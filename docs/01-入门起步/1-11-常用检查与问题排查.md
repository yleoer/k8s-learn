# 常用检查与问题排查

本节整理集群初始化后的常用检查命令和高频问题。

## 常用检查命令

节点和 Pod：

```bash
kubectl get nodes -o wide
kubectl get pods -A -o wide
kubectl describe node <node-name>
```

控制平面：

```bash
kubectl get --raw='/readyz?verbose'
kubectl get --raw='/livez?verbose'
kubectl -n kube-system get pods -o wide
kubectl -n kube-system logs -l component=kube-apiserver
```

containerd：

```bash
sudo systemctl status containerd --no-pager
sudo crictl info
sudo crictl ps -a
sudo crictl images
```

kubelet：

```bash
sudo systemctl status kubelet --no-pager
sudo journalctl -u kubelet -xe --no-pager
```

网络：

```bash
ip addr
ip route
sysctl net.ipv4.ip_forward
lsmod | grep br_netfilter
```

Calico：

```bash
kubectl get pods -n calico-system -o wide
kubectl get installation default -o yaml
kubectl get ippool -A -o yaml
```

## kubeadm init 提示 swap 未关闭

检查：

```bash
swapon --show
cat /etc/fstab | grep -i swap
```

处理：

```bash
sudo swapoff -a
sudo vim /etc/fstab
```

注释 swap 行后重新执行 kubeadm。

## 节点一直 NotReady

常见原因：

- CNI 没有安装或安装失败。
- `--pod-network-cidr` 和 Calico IPPool CIDR 不一致。
- containerd 未正常运行。
- kubelet 异常。

检查：

```bash
kubectl get pods -A -o wide
kubectl describe node <node-name>
sudo systemctl status containerd --no-pager
sudo systemctl status kubelet --no-pager
sudo journalctl -u kubelet -xe --no-pager
```

## 镜像拉取失败

检查 containerd 是否启用了 registry hosts 配置：

```bash
grep -n "config_path" -A 3 /etc/containerd/config.toml
sudo systemctl restart containerd
```

测试：

```bash
sudo crictl pull registry.k8s.io/pause:3.10
sudo crictl pull docker.io/library/nginx:latest
```

如果仍失败，换用可访问的镜像源或检查 DNS、代理和防火墙。

## kubelet cgroup driver 不匹配

Kubernetes 推荐 kubelet 和容器运行时都使用 systemd cgroup driver。检查 containerd：

```bash
grep -n "SystemdCgroup" /etc/containerd/config.toml
```

应为：

```text
SystemdCgroup = true
```

修改后重启：

```bash
sudo systemctl restart containerd
sudo systemctl restart kubelet
```

## 重新初始化实验集群

如果是实验环境，需要彻底重来，可以在相关节点执行：

```bash
sudo kubeadm reset
sudo rm -rf /etc/cni/net.d
sudo rm -rf "$HOME/.kube"
sudo systemctl restart containerd
```

该操作会清理当前节点上的 Kubernetes 配置。生产环境不要直接执行。
