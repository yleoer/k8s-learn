# 入门起步

本章是全书的起点，用于搭建一套可运行、可验证、便于反复练习的 Kubernetes 实验集群。当前阶段以基础环境、运行时组件和集群初始化为前置背景。

本章围绕 kubeadm、containerd、Calico、Metrics Server 和基础排障链路展开，形成从主机准备到集群验证的完整路径。后续 Pod、Service、Ingress、存储、调度和可观测性等记录均基于这一实验环境。
