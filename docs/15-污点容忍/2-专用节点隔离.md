# 专用节点隔离

专用节点通常用于高性能计算、受监管业务或基础设施组件。可靠隔离至少包含节点标签、`NoSchedule` 污点、目标工作负载的节点亲和性和精确容忍四部分。

## 预留支付节点

```bash
kubectl label no <node-name> dedicated.example.com/team=payments
kubectl taint no <node-name> dedicated.example.com/team=payments:NoSchedule
```

```yaml [payments-api.yaml]
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: payments-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: payments-api
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: dedicated.example.com/team
                    operator: In
                    values:
                      - payments
      tolerations:
        - key: dedicated.example.com/team
          operator: Equal
          value: payments
          effect: NoSchedule
      containers:
        - name: api
          image: nginx:1.31-alpine
          ports:
            - containerPort: 80
```

```bash
kubectl create -f payments-api.yaml
kubectl get po -l app.kubernetes.io/name=payments-api -o wide
```

污点阻止不具备容忍的 Pod 进入；节点亲和性防止该应用被调度到其他节点。两项缺一不可。

> [!WARNING]
> 污点不等于安全授权。能够创建任意 Pod 的主体仍可能添加容忍或引用特权配置。专用节点的权限边界还需要 RBAC、Pod Security 与节点层隔离共同保证。

## 参考

- [专用节点](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#example-use-cases)
